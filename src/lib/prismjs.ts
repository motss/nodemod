import Prism from 'prismjs';

const {
  Token,
  highlight,
  highlightAll,
  highlightAllUnder,
  highlightElement,
  hooks,
  languages,
  plugins,
  tokenize,
  util,
} = Prism;

export {
  Token,
  highlight,
  highlightAll,
  highlightAllUnder,
  highlightElement,
  hooks,
  languages,
  plugins,
  tokenize,
  util,
};
export default Prism;
